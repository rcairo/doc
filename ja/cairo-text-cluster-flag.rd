= module Cairo::TextClusterFlag

((*Since 1.8*)): テキストクラスタ対応のプロパティを指定しま
す。

== Constants

--- BACKWARD

     クラスタ配列内のクラスタを後ろから順にグリフ配列内のグ
     リフに対応させます。

== See Also

  * Cairo::UserFontFace::TextToGlyphsData

== ChangeLog

  * 2008-09-26: kou: スタート。
