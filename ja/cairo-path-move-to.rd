= class Cairo::PathMoveTo

移動する操作を表すパスデータです。

== Object Hierarchy

* Object
  * ((<Cairo::PathData>))
    * Cairo::PathMoveTo

== Class Methods

--- Cairo::PathMoveTo.new(x, y)
--- Cairo::PathMoveTo.new(point)

     (((|x|)), ((|y|)))あるいは((|point|))へ移動するパスデー
     タを生成します。

     * x: 新しい位置のX座標
     * y: 新しい位置のY座標

     * point: 新しい位置

     * Returns: Cairo::PathMoveToオブジェクト。

== See Also

  * Index

== ChangeLog

  * 2007-05-22: kou: スタート。
