= module Cairo::Context::Path::Point

点に関する便利なメソッドを提供するモジュールです。通常は使う
ことはありません。

cairoにはないrcairoでの拡張です。

== Module Functions

--- Cairo::Context::Path::Point.distance(a, b)

     点aと点bの距離を計算します。

     * a: 点を表す配列1: [x1, y1]
     * b: 点を表す配列2: [x2, y2]
     * Returns: 点aと点bの距離

== See Also

  * ((<Cairo::Context::Path>))
  * ((<Index>))

== ChangeLog

  * 2007-04-30: kou: スタート。
