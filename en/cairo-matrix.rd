= class Cairo::Matrix

== Object Hierarchy

* Object
  * Cairo::Matrix

== Class Methods

--- Cairo::Matrix.identity

     * Returns: self: ((*FIXME*))

--- Cairo::Matrix.new

     * Returns: self: ((*FIXME*))

--- Cairo::Matrix.rotate

     * Returns: self: ((*FIXME*))

--- Cairo::Matrix.scale

     * Returns: self: ((*FIXME*))

--- Cairo::Matrix.translate

     * Returns: self: ((*FIXME*))

== Instance Methods

--- *

     * Returns: self: ((*FIXME*))

--- ==

     * Returns: self: ((*FIXME*))

--- clone

     * Returns: self: ((*FIXME*))

--- dup

     * Returns: self: ((*FIXME*))

--- identity!

     * Returns: self: ((*FIXME*))

--- invert

     * Returns: self: ((*FIXME*))

--- invert!

     * Returns: self: ((*FIXME*))

--- multiply

     * Returns: self: ((*FIXME*))

--- multiply!

     * Returns: self: ((*FIXME*))

--- rotate

     * Returns: self: ((*FIXME*))

--- rotate!

     * Returns: self: ((*FIXME*))

--- scale

     * Returns: self: ((*FIXME*))

--- scale!

     * Returns: self: ((*FIXME*))

--- set

     * Returns: self: ((*FIXME*))

--- set_x0

     * Returns: self: ((*FIXME*))

--- set_xx

     * Returns: self: ((*FIXME*))

--- set_xy

     * Returns: self: ((*FIXME*))

--- set_y0

     * Returns: self: ((*FIXME*))

--- set_yx

     * Returns: self: ((*FIXME*))

--- set_yy

     * Returns: self: ((*FIXME*))

--- to_a

     * Returns: self: ((*FIXME*))

--- to_s

     * Returns: self: ((*FIXME*))

--- transform_distance

     * Returns: self: ((*FIXME*))

--- transform_point

     * Returns: self: ((*FIXME*))

--- translate

     * Returns: self: ((*FIXME*))

--- translate!

     * Returns: self: ((*FIXME*))

--- x0

     * Returns: self: ((*FIXME*))

--- x0=

     * Returns: self: ((*FIXME*))

--- xx

     * Returns: self: ((*FIXME*))

--- xx=

     * Returns: self: ((*FIXME*))

--- xy

     * Returns: self: ((*FIXME*))

--- xy=

     * Returns: self: ((*FIXME*))

--- y0

     * Returns: self: ((*FIXME*))

--- y0=

     * Returns: self: ((*FIXME*))

--- yx

     * Returns: self: ((*FIXME*))

--- yx=

     * Returns: self: ((*FIXME*))

--- yy

     * Returns: self: ((*FIXME*))

--- yy=

     * Returns: self: ((*FIXME*))

== See Also

== ChangeLog


